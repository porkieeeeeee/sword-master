import styled from "@emotion/styled";

const MainPage = () => {
    return (
        <Container>
            <p>main</p>
        </Container>
    );
};

const Container = styled.div`
    width: 100%;
    height: 100%;
`;

export default MainPage;
